kind = "mobile"
previewPath = "/mobile/"
title = "MK DOC Mobile"
version = "1.0.0"
id = "artifacts/mk-doc-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 23626

[services.development]
run = "pnpm --filter @workspace/mk-doc-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mk-doc-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/mk-doc-mobile", "run", "serve" ]

[services.env]
PORT = "23626"
BASE_PATH = "/mobile/"
